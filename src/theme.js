// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4caf50', // Зеленый цвет из вашего логотипа
    },
    secondary: {
      main: '#212121', // Черный цвет из вашего логотипа
    },
  },
});

export default theme;
